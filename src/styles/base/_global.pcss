@import '../utils/_variables.pcss';

body,
html {
  height: 100%;
}

body {
  background-image: linear-gradient(
    to right,
    var(--color-dark),
    var(--color-blue),
    var(--color-dark)
  );
  text-align: center;

  * {
    box-sizing: border-box;
  }
}
